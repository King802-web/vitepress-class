{
  "dependencies": {
    "vitepress": "^1.6.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.0"
  },
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "dev": "vitepress dev",
    "build": "npx vitepress build",
    "vercel-build": "node vercel-build.js",
    "deploy:unix": "bash deploy.sh",
    "deploy:win": ".\\deploy.bat",
    "deploy": "npm run deploy:win",
    "deploy:vercel:win": ".\\deploy-vercel.ps1",
    "deploy:vercel:unix": "bash deploy-vercel.sh",
    "deploy:vercel": "npm run deploy:vercel:win",
    "deploy:render:win": ".\\deploy-render.ps1",
    "deploy:render:unix": "bash deploy-render.sh",
    "deploy:render": "npm run deploy:render:win"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
